# Default: frontend-only mode (no backend needed)
# VITE_AI_MODE=local
# VITE_AI_MODE=api
VITE_AI_MODE=api-stream
# VITE_API_BASE_URL — leave empty for production (nginx proxies /api)
# Set to http://localhost:8000 for local development
VITE_API_BASE_URL=
